# Data
dataset: vsd
# Legacy HDF5 path
vsd_hdf5_path: "G:/My Drive/HDF5_DATA_AFTER_PREPROCESSING2/vsd_video_data.hdf5"
normalize: true
# Split data
split_csv_path: "G:/My Drive/Gonda_research_Hamutal_2024/Ossnat/VSD_FM/Data/ProcessedData/DataFromYarden/splits/split_v1_seed17_strat_monkey.csv"
stats_json_path: "G:/My Drive/Gonda_research_Hamutal_2024/Ossnat/VSD_FM/Data/ProcessedData/DataFromYarden/splits/stats_v1_seed17_strat_monkey.json"
processed_root: "G:/My Drive/Gonda_research_Hamutal_2024/Ossnat/VSD_FM/Data/ProcessedData/DataFromYarden/splits" 
normalization_type: baseline_zscore
baseline_frame: 20
frame_start: 1
frame_end: 100
frames: 100
height: 100
width: 100
channels: 1
batch_size: 8
num_workers: 2
train_split: 0.8  # 80% for training, 20% for validation
window_size: 0  # 0 disables windowing; >0 uses non-overlapping windows (T per sample)


# Task
task: mae # mae | recon (plain autoencoder)
mask_ratio: 0.5
patch_size: [4, 8, 8] # t, h, w


# Model
model: cnn3d # cnn3d (encoder) used inside MAE
embed_dim: 256


# Optim
epochs: 5 # keep tiny for smoke test
lr: 0.001
weight_decay: 0.05
seed: 42
log_dir: logs
ckpt_dir: checkpoints
